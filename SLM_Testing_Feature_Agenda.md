## SLM Model Testing Features

1. Accuracy Measuring system(Taking a higher model's performance as the benchmark to calculate against)
2. Multi model parallel running system
3. Performance measuring system
4. Combine all of the above to make a complete app where we can toggle select models, a resume and run them paralley to find accuracy with reference to a larger model's performance
